# web3-miner — working rules

## Preview / dev server — DO NOT auto-start anything
- **NEVER start the preview tool, never auto-launch a browser preview, never take
  preview screenshots.** The user watches only on their own machine.
- **NEVER auto-start the Vite dev server either.** The user starts it himself when
  he wants to watch. The IDE auto-launch was disabled on purpose: `.claude/launch.json`
  has an empty `configurations: []` (backup at `.claude/launch.json.disabled-bak`).
  Do not re-add a launch config and do not run vite in the background unasked.
- After making changes: just say "обнови localhost:5189" (only if the user already
  has the server running). Do NOT verify changes yourself by launching anything.

### Manual start command (ONLY if the user explicitly asks to run it)
```
cd ~/work/web3-miner/frontend && npm run dev -- --port 5189
```
→ http://localhost:5189

## No "Claude" attribution — ever
- **Nothing may carry a Claude/Anthropic/AI label.** Everything is the user's work,
  authored from his name.
- Git: **NO `Co-Authored-By: Claude` trailer**, no "Generated with Claude Code",
  no Claude mention in commit messages, PR titles/bodies, branch names.
- Code/docs: no "generated by", "written by Claude", AI banners or comments.
- Commit author stays the user: `EugeneWay <relax211@gmail.com>`.

## Project
- Canonical project lives here: `~/work/web3-miner/` (frontend = Phaser 3 + Vite).
- The copy under `~/work/marketplace-games/games/robo-miner/` is LEGACY — do not edit it.
